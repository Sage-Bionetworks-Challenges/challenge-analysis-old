version: "3.8"

services:
  rstudio:
    image: sagebionetworks/rstudio:4.0.5
    build:
      context: .
      dockerfile: Dockerfile
    container_name: rstudio
    env_file:
      - .env
    volumes:
      - .:/home/rstudio/challenge-analysis:rw
    ports:
      - "${HOST_PORT}:8787"
    command: rstudio